ctrl_interface=/var/run/wpa_supplicant

# Define the AP network block
network={
    ssid="MyAPName"
    mode=2 
    key_mgmt=WPA-PSK
    proto=RSN
    pairwise=CCMP
    group=CCMP
    psk="MyAPPassword"
}